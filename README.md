# 🛡️ ITB-SCREEN-RECORDER

מערכת Enterprise ללכידת מסך, הקלטה אקטיבית ושידור נתונים לשרת מרכזי (C2), המותאמת לסביבות ארגוניות מבודדות (Air-Gapped)[cite: 7]. המערכת מספקת צפייה חיה והקלטות רציפות תוך אפס עומס מעבד (Zero CPU Load) בזכות האצת חומרה מתקדמת[cite: 8]. המערכת כתובה ב-.NET 8 ותומכת באופן מובנה ב-Windows וב-Linux[cite: 7, 8].

## 🏗️ מבנה הפרויקטים (System Architecture)

* **ITB-SCREEN-RECORDER.Core:** ספרייה משותפת המהווה מקור אמת יחיד (Single Source of Truth) לכלל המודלים, ה-DTOs ותקשורת ה-IPC מבוססת ה-TCP, ללא תלות במערכת ההפעלה[cite: 6, 12].
* **ITB-SCREEN-RECORDER.Server:** אורקסטרטור ה-Middleware[cite: 7]. בסביבת לינוקס, המערכת נארזת ומופעלת באמצעות **Podman** להבטחת בידוד תהליכים מושלם[cite: 6]. השרת מנהל את ה-MediaMTX, חותך ושומר וידאו באחסון ה-NetApp הארגוני, ומגיש את ממשק השליטה ב-React C2 Dashboard[cite: 7].
* **ITB-SCREEN-RECORDER.AgentService:** שירות ניהול הפועל ברקע (Session 0 בווינדוס או Root Daemon בלינוקס) המשמש כ-Watchdog[cite: 1, 6]. הרכיב מקבל פקודות מהשרת ומזניק את תהליך הלכידה לתוך ה-Session הגרפי עם התחברות המשתמש (Logon)[cite: 12].
* **ITB-SCREEN-RECORDER.AgentWorker:** הרכיב הפעיל ב-Session 1 המנהל את מנוע הדחיסה (Compression Engine) הבלעדי – **FFmpeg**[cite: 12]. הוא מנצל האצת חומרה (NVENC/QSV) באפס עומס (או מבצע Fallback בטוח לקידוד מעבד), וכולל אוגר Offline השומר את ההקלטות כקובץ FLV מקומי בעת ניתוק מהרשת[cite: 1, 8, 12].

## 🚀 פריסה והתקנה (CI/CD Deployments)

* **סביבת Windows:** תהליך האוטומציה ב-GitHub Actions מקמפל חבילות התקנה טבעיות מסוג `.msi` להפצה שקטה באמצעות מערכות כדוגמת SCCM או ManageEngine[cite: 6].
* **סביבת Linux:** השרת המרכזי נפרס דרך קונטיינר Podman, ואילו סוכני הקצה נפרסים באמצעות העתקת קבצים פשוטה, מתן הרשאות הרצה (`chmod +x`) והגדרת שירות systemd[cite: 6].
* **עצמאות מוחלטת:** התוצרים הם Self-Contained Binaries הכוללים בילד FFmpeg סטטי, ללא תלות באינטרנט או בשדרוגי ספריות מערכת שוברי-תלויות[cite: 6, 8].

## ⚖️ הבהרה משפטית ורישוי (Disclaimer: FFmpeg)

* **תלות טכנית:** המערכת פועלת בשילוב מוחלט עם מנוע **FFmpeg** לצורך דחיסת וידאו ומיזוג שמע, ואינה מתפקדת בשום אופן בלעדיו[cite: 6, 12].
* **תאימות GPL (Mere Aggregation):** קוד ה-C# המסחרי נשמר כקוד סגור ואינו מטמיע או משתמש בספריות ה-DLL של FFmpeg בתוך מרחב הזיכרון שלו[cite: 6].
* **הפרדת תהליכים:** מנוע ה-FFmpeg מופעל כקובץ בינארי עצמאי (External Sub-process) המקבל נתונים גולמיים דרך צינורות תקשורת סטנדרטיים של מערכת ההפעלה (TCP Sockets/Pipes)[cite: 6, 12].
* **קוד מקור ורשיון:** פרויקט FFmpeg מופץ תחת רישיון הקוד הפתוח GPL. אתר הפרויקט להורדה ולעיון: https://ffmpeg.org[cite: 6].